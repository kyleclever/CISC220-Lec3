/*
 * 1_list.cpp
 *
 *  Created on: Sep 27, 2016
 *      Author: kylew
 */

/*	int x [7];				X[3]
 *  int *y = new int [7];	y[3]
 */

/*
 *
 *NOTE: lISTES:
  	  1. List has a size
  	  2. Data in a list can be duplicated
  	  3. all elements in the list are in same data type

push(x)-add to end of list
insert(x,k) adds item x to list at kth position
remove (Node *node) 每 removes a node from the list
removekth(int kth) 每 removes the node at the kth position from the list
pop() 每 removes the last element from the list
size()  - gives you number of elements in list
isEmpty() 每 returns true iff the list is empty
find(x) 每 return the position of x in the list (usually -1 if not in list)
findkth(k) 每 return the item at the kth position in the list
printList()
 *
 *
 */

